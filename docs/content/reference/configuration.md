---
title: "Configuration"
description: "Environment variables and global flags."
weight: 20
---

pypi aims to need almost no configuration. As you add settings,
document each one here with its default.

## Global flags

| Flag | Meaning |
|---|---|
| `--help` | Help for any command |
| `--version` | Print the version |

## Environment variables

None yet. Add them here as the tool grows, prefixed `pypi_` in upper
case (for example `pypi_TOKEN`).
